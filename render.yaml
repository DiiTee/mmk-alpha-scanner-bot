services:
  - type: web
    name: mmk-alpha-scanner
    env: python
    region: frankfurt  # or your preferred region
    plan: free
    buildCommand: ""
    startCommand: "python main.py"
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false